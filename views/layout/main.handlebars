<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>{{title}}</title>
  <style>
    body{font-family:system-ui,Arial;margin:24px auto;max-width:900px}
    nav a{margin-right:12px}
    input,button,textarea{padding:8px;margin:4px 4px 8px 0}
    .card{border:1px solid #ddd;border-radius:8px;padding:12px;margin:8px 0}
    .thumb{max-width:100px;display:inline-block;margin-right:8px}
  </style>
</head>
<body>
  <nav>
    <a href="/">Home</a> |
    <a href="/realtimeproducts">RealTimeProducts</a>
  </nav>
  {{{body}}}
</body>
</html>
